# MNIST 손글씨 인식 과제 보고서

## 0. 반·팀원


| 항목     | 내용            |
| --- | --- |
| **반** | 301 |
| **팀명** | 4조 |
| **팀원** | 김규민, 김용, 김주형, 백승진 |


---

## 1. 실험 목적

MNIST 10-class 분류를 **NumPy만으로 구현한 신경망**으로 수행하고, 테스트 정확도와 학습 과정을 보고합니다.

---

## 2. 모델 구조


| 구분      | 내용                                                               |
| ------- | ---------------------------------------------------------------- |
| **입력** | 784차원 입력. 28×28 MNIST 이미지를 flatten하고 0~1 범위로 정규화 |
| **은닉층 수** | 2개 |
| **은닉층 차원** | 512, 256 |
| **은닉층 구성** | Affine → BatchNorm → ReLU → Dropout |
| **출력층** | Affine(10) → Softmax |
| **활성화 함수** | ReLU |
| **정규화** | BatchNorm 사용 |
| **규제** | Dropout 사용 (비율 0.5) |

모델 전체 구조는 다음과 같다.

```
입력 784
  → Affine(784→512) → BatchNorm → ReLU → Dropout(0.5)
  → Affine(512→256) → BatchNorm → ReLU → Dropout(0.5)
  → Affine(256→10)  → Softmax
```

가중치는 ReLU 활성화 함수에 맞춰 **He initialization**을 사용했고, bias는 0으로 초기화했다.

### 파라미터 수

| Layer | 계산 | 파라미터 수 |
| --- | --- | --- |
| Affine1 | 784×512 + 512 | 401,920 |
| BatchNorm1 | gamma + beta | 1,024 |
| Affine2 | 512×256 + 256 | 131,328 |
| BatchNorm2 | gamma + beta | 512 |
| Affine3 | 256×10 + 10 | 2,570 |
| **합계** |  | **537,354** |
---

## 3. 학습 설정


| 항목                 | 값           |
| ------------------ | ----------- |
| 옵티마이저              | Adam        |
| 학습률 (lr)           | 0.001       |
| epochs             | 20          |
| batch_size         | 128         |
| Dropout 비율         | 0.5         |
| BatchNorm momentum | 0.9         |
| 가중치 초기화            | He initialization (bias 0) |
| Loss | Cross Entropy Loss |
| 데이터셋 | MNIST train 60,000장 / test 10,000장 |

학습 루프는 매 epoch마다 학습 데이터를 무작위로 섞고, mini-batch 단위로 다음 순서를 반복했다.

```
Forward → Cross Entropy Loss 계산 → Backward → Adam 업데이트
```

---

## 4. 실험 환경

| 항목 | 내용 |
| --- | --- |
| 실행 환경 | Google Colaboratory <br> - T4 GPU <br> - 런타임 버전 : 25년 7월 |
| Python | 3.11.13 |
| NumPy | 2.0.2 |
| Matplotlib | 손실 커브 시각화 함수 제공 |
| 데이터셋 | MNIST train 60,000장 / test 10,000장 |
| 학습 시간 | 약 4분 |

---

## 5. 결과

### 5.1 기본 테스트 결과

<img src="https://github.com/user-attachments/assets/e766594f-6778-44e0-8bb5-7bd0b77870b1" width="70%"></img>

| 항목           | 값              |
| ------------ | -------------- |
| **테스트 정확도**  | 98.40%    |
| **총 파라미터 수** | 537,354 |
| **Train loss** | 0.223716 |
| **학습 시간** | 약 4분 |
| **95% 기준 대비** | +3.40%p |
| **97% 권장 기준 대비** | +1.40%p |

기본 설정(Adam, lr=0.001, Dropout 0.5, BatchNorm)에서 테스트 정확도 98.40%를 얻었다(권장 기준 97%를 1.40%p 상회).

초반(1~5 epoch)에 loss가 급격히 떨어진 뒤, 이후로는 완만하게 감소하며 0.22 부근에서 안정화됐다.

<br>

아래 5.2~5.5는 단일 하이퍼파라미터만 바꾸고 나머지는 기본 설정으로 고정한 실험 결과다. 정확도와 최종 train loss는 모두 실험 로그의 실측값이다.

### 5.2 학습률 비교

<img src="https://github.com/user-attachments/assets/b4444180-284a-4938-b1a7-fbfbb407b646" width="70%"></img>

| 학습률 | 최종 train loss | 테스트 정확도 |
| --- | --- | --- |
| 0.01 | 0.286832 | 98.31% |
| **0.001 (채택)** | **0.223716** | **98.40%** |
| 0.0001 | 0.552388 | 97.62% |

- **lr=0.001이 최저 loss·최고 정확도**로, 세 값 중 가장 균형이 좋다.
- lr=0.01은 초기 수렴은 가장 빠르지만, 최종 loss(0.286832)가 0.001(0.223716)보다 높은 지점에서 정체한다. 갱신 폭이 커서 최저점 부근에서 미세하게 진동하는 것으로 보인다.
- lr=0.0001은 최종 loss가 0.552388로 가장 높아, 20 epoch 안에 충분히 수렴하지 못했다. Epoch 1 loss도 가장 높게 출발해 끝까지 다른 두 설정을 따라잡지 못했다. 학습률이 너무 작아 발생하는 **과소적합(underfitting) 신호**로 파악된다.

### 5.3 Dropout 적용 전후 비교

<img src="https://github.com/user-attachments/assets/8436a018-478e-437c-8dbb-08efd9663483" width="70%"></img>

| 설정 | 최종 train loss | 테스트 정확도 |
| --- | --- | --- |
| **Dropout OFF** | 0.216148 | **98.60%** |
| Dropout ON (0.5) | 0.214801 | 98.41% |

- 최종 train loss는 0.216148 vs 0.214801로 사실상 동일하다(차이 0.0013).
- **그런데 테스트 정확도는 Dropout OFF가 0.19%p 더 높다.** 학습 loss가 같은데 OFF의 일반화 성능이 더 나았다는 뜻이다. 즉 이 모델·데이터셋 조합에서 Dropout 0.5는 정확도를 끌어올리지 못했고, 오히려 소폭 떨어뜨렸다.

### 5.4 BatchNorm 적용 전후 비교

<img src="https://github.com/user-attachments/assets/53bb408c-2216-4c1e-b4d9-719c41a4b9a5" width="70%"></img>

| 설정 | 최종 train loss | 테스트 정확도 |
| --- | --- | --- |
| BatchNorm OFF | 0.216357 | 98.40% |
| **BatchNorm ON** | 0.224030 | **98.48%** |

- 최종 loss는 OFF가 오히려 약간 낮고(0.216357 < 0.224030), 정확도는 ON이 +0.08%p 높다. 두 지표가 서로 반대 방향이라 어느 쪽도 우열을 단정하기 어려운 수준이다.
- 이 차이(loss 0.008, 정확도 0.08%p)는 뒤에서 설명할 실행 간 편차 범위 안에 들어간다. 이 실험만으로 BatchNorm의 효과를 단정하기는 어렵다.

### 5.5 Optimizer(SGD vs Adam) 비교

<img src="https://github.com/user-attachments/assets/28831426-dcd0-4bae-8664-afffd30b0712" width="70%"></img>

| 옵티마이저 | 최종 train loss | 테스트 정확도 |
| --- | --- | --- |
| SGD (lr=0.01) | 1.480159 | 94.50% |
| **Adam (lr=0.001)** | **0.227734** | **98.43%** |

- **네 실험 중 차이가 가장 뚜렷하다.** Adam이 SGD보다 정확도 +3.93%p 높다.
- SGD는 최종 loss가 1.480159로, 20 epoch이 지나도 1.5 부근에 머물러 아직 수렴이 끝나지 않았다. Adam(0.227734)의 약 6.5배다.
- Adam은 동일 epoch 안에 loss를 0.227734까지 빠르게 떨어뜨렸다. 파라미터별로 학습률을 적응적으로 조정하는 Adam의 특성이 이 차이를 만든다.

---

## 6. 회고

### 6.1 수렴 및 과적합 여부

- **수렴**: 기본 설정의 train loss는 0.22까지 떨어진 뒤 곡선이 평탄해졌고, 발산이나 진동 없이 안정적으로 수렴했다.

- **과적합(overfitting)**: 강한 과적합 신호는 보이지 않는다. 근거는 두 가지다. 첫째, train loss(0.22)가 낮고 테스트 정확도(98.40%)도 높다. 과적합이 심한 모델은 훈련 성능은 좋지만 처음 보는 데이터에서 성능이 크게 떨어지는데, 이 모델은 두 지표가 모두 양호하다. 둘째, Dropout을 껐을 때 테스트 정확도가 오히려 올랐다(5.3). 과적합 중인 모델이라면 규제를 제거했을 때 성능이 떨어졌어야 한다. 결과는 반대였으므로, 20 epoch 학습에서는 규제가 필요할 만큼 과적합이 진행되지 않았다고 판단하였다.

- **과소적합(underfitting)**: lr=0.0001 실험(5.2)은 명확한 과소적합 사례다. 학습률이 너무 작아 정해진 epoch 안에 최저점에 도달하지 못했다.

### 6.2 하이퍼파라미터 변경 시도와 결과

- **학습률**: 0.01 / 0.001 / 0.0001 중 **0.001이 최적**이었다. 너무 크면(0.01) 후반 수렴이 불안정하고, 너무 작으면(0.0001) 수렴이 느려 과소적합이 발생하는 전형적인 trade-off를 확인했다.

- **Optimizer**: SGD → Adam 교체가 **가장 효과가 컸다(+3.93%p)**. 동일 epoch 예산에서 Adam이 압도적으로 빠르게 수렴했다. 네 실험 중 결론이 가장 분명한 항목이다.

- **BatchNorm**: ON이 +0.08%p로 미세하게 높았으나, 6.3에서 확인한 실행 간 편차 범위 안에 들어오는 수치라 효과를 단정하기 어렵다. BatchNorm의 본래 이점인 학습 안정화는 깊은 망에서 두드러지는데, 은닉층 2개짜리 얕은 망에서는 그 효과가 잘 드러나지 않은 것으로 보인다.

- **Dropout**: Dropout OFF(98.60%)가 ON(98.41%)보다 높았다. 과적합이 거의 없는 상황에서 drop 비율 0.5는 억제할 대상 없이 학습만 방해한 셈이다. 더 긴 학습이나 더 큰 모델에서는 Dropout이 유효할 수 있으므로, 현재 조건에 한정된 결과로 본다.

### 6.3 실행 간 편차에 대한 주의

동일한 기본 설정(Adam, lr=0.001, Dropout 0.5, BatchNorm)이 네 실험에 공통으로 포함됐는데, 실측값이 실행마다 조금씩 달랐다. 테스트 정확도는 98.40 / 98.41 / 98.43 / 98.48%, 최종 train loss는 0.2148 / 0.2237 / 0.2240 / 0.2277로 분포한다. random seed를 고정하지 않아 발생한 실행 간 편차로 보이며, 정확도 약 ±0.1%p · loss 약 ±0.01 수준이다. 이 범위 안의 차이는 효과로 단정하기보다 편차 가능성을 함께 고려해 해석했다.

### 6.4 정리

이번 실험에서 정확도에 실질적으로 기여한 요소는 **Optimizer 선택(Adam)과 적절한 학습률(0.001)** 두 가지였다. Dropout · BatchNorm 같은 규제 · 정규화 기법은 얕은 망에 짧은 학습이라는 조건에서 뚜렷한 이득을 주지 못했고, Dropout은 오히려 소폭 손해였다. 최적화 설정이 규제 설정보다 결과를 더 크게 좌우한 실험이었다.